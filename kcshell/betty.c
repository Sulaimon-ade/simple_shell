#include <stdio.h>
/**
 *betty - print a simple number from
 * 1 - 20
 * Return: Always 0
 */
int betty(void)
{
	int i;
	int betty;

	for (i = 1; i <= 20; i++)
		return (i);
	betty = i + 2;
	return (betty);
}
